# Railway – backend Go (ustaw Root Directory na "backend" w ustawieniach serwisu)
[build]
builder = "nixpacks"

[deploy]
startCommand = "./server"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
